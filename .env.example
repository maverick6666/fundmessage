# ===========================================
# 로컬 테스트용 환경변수
# 이 파일을 복사해서 .env로 만들고 값을 채우세요
# ===========================================

# 데이터베이스 설정 (Docker Compose용)
DB_USER=funduser
DB_PASSWORD=fundpass123
DB_NAME=fundmessenger

# 백엔드 직접 실행 시 필요 (Docker Compose 사용 시 자동 설정됨)
DATABASE_URL=postgresql://funduser:fundpass123@localhost:5432/fundmessenger

# JWT 설정
SECRET_KEY=your-super-secret-key-change-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS 설정
CORS_ORIGINS=http://localhost:5173,http://localhost:5174,http://127.0.0.1:5173

# 프론트엔드 API URL
VITE_API_URL=http://localhost:8000/api/v1
VITE_WS_URL=http://localhost:8000

# ===========================================
# 외부 API 키 (테스트에 필요한 것만 입력)
# ===========================================

# OpenAI API (AI 노트/보고서 생성, 뉴스데스크)
OPENAI_API_KEY=sk-xxx
OPENAI_MODEL=gpt-4o-mini

# 네이버 검색 API (뉴스 크롤링)
NAVER_CLIENT_ID=
NAVER_CLIENT_SECRET=

# 한국투자증권 API (시세 조회)
KIS_APP_KEY=
KIS_APP_SECRET=
KIS_ACCOUNT_NO=

# Binance API (공개 API는 키 불필요)
BINANCE_API_KEY=
BINANCE_API_SECRET=
